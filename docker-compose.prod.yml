version: "3.8"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://10.0.40.10:3001}
    container_name: umg_frontend_prod
    ports:
      - "3001:80"
    environment:
      - NODE_ENV=production
